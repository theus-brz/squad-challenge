import styled from 'styled-components';

export const PageFooterContainer = styled.footer`
  background-color: ${(props) => props.theme.pageFooter};
  padding: 15px;
  display: flex;
  justify-content: center;
`;
